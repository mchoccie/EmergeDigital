import { render, screen } from "@testing-library/react";
import DropdownButton from "Header/DropdownButton";

// Can't write tests for same reason as Header

test("dummy test", () => {
  expect(true);
});
